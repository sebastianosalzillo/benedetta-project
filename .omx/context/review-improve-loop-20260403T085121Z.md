# Context Snapshot — review-improve-loop

- task statement: Review the Avatar ACP Desktop repo and improve it in a Ralph-style loop, with Team-mode requested by the user.
- desired outcome: Deliver at least one safe, verified improvement batch informed by the existing review findings, without destabilizing the current uncommitted refactor work.
- known facts/evidence:
  - User explicitly invoked `$ralph $team`.
  - Ralph planning gate is active; `.omx/plans/` was initially missing.
  - `tmux` is not installed/available on this Windows environment, so interactive `omx team` runtime is likely blocked.
  - `omx --version` succeeds (`oh-my-codex v0.11.12`).
  - `docs/shared/agent-tiers.md` is not present in this repo.
  - Repo has existing uncommitted changes in Electron and React files; avoid destructive or overlapping refactors where possible.
  - Existing `quality-review.md` identifies high-risk large-scope issues plus smaller UI/accessibility improvements.
  - Package scripts available: `npm run build`, `npm run test:unit`, `npm run test:smoke`.
- constraints:
  - Must follow Ralph + Team instructions from AGENTS/skill docs.
  - No new dependencies unless explicitly requested.
  - Keep diffs small, reviewable, and reversible.
  - Need fresh verification evidence before claiming completion.
  - Must not overwrite unrelated user changes.
- unknowns/open questions:
  - Whether any team runtime path can work without tmux in this environment.
  - Which improvement batch yields the best value/risk tradeoff given existing local modifications.
  - Whether current renderer files already partially address some review findings.
- likely codebase touchpoints:
  - `src/App.jsx`
  - `src/index.css`
  - `src/components/AvatarChat.jsx`
  - `src/components/SettingsPanel.jsx`
  - possibly `quality-review.md` for refreshed evidence/reporting
