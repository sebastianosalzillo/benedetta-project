# Context Snapshot: rollup-optional-dependency-fix

- Task statement: fix the Windows Rollup native optional dependency failure so the local Vite/Electron project starts correctly.
- Desired outcome: `npm run dev:vite` starts without `Cannot find module @rollup/rollup-win32-x64-msvc`, and project verification commands pass.
- Known facts/evidence:
  - Reproduction confirmed with `npm run dev:vite` on 2026-04-03 UTC.
  - Failure occurs inside `node_modules/rollup/dist/native.js`.
  - `package-lock.json` already contains `node_modules/@rollup/rollup-win32-x64-msvc` and Rollup lists it in `optionalDependencies`.
  - `node_modules/@rollup/rollup-win32-x64-msvc` is currently absent.
  - Repo is in a dirty state; avoid reverting unrelated user changes.
- Constraints:
  - Use Ralph workflow with verification and architect sign-off.
  - No new dependency should be introduced unless necessary.
  - Keep changes reversible and scoped to solving the installation/runtime issue.
- Unknowns/open questions:
  - Whether a clean reinstall alone resolves the missing package.
  - Whether additional Node 24 / Vite / Electron issues appear after reinstall.
  - Whether reinstall regenerates lockfile metadata in a meaningful way.
- Likely codebase touchpoints:
  - `package-lock.json`
  - `node_modules/`
  - `package.json`
  - `.omx/plans/`
  - `.omx/state/sessions/omx-1775214080030-jo81ua/ralph-progress.json`
